# Beacon短信平台系统 - 项目介绍文档

## 一、项目概述

### 1.1 项目简介
**Beacon短信平台系统**是一个企业级分布式短信服务平台，主要用于处理大并发量的短信发送业务。系统采用微服务架构，通过CMPP协议与运营商网关对接，实现短信的发送、状态报告接收、日志存储等核心功能。

### 1.2 核心功能
- ✅ 短信发送：支持单条/批量短信发送
- ✅ 通道管理：支持多通道、权重路由、通道切换
- ✅ 策略过滤：黑名单、敏感词、限流、费用校验等
- ✅ 状态报告：接收运营商状态报告并推送给客户
- ✅ 日志存储：基于Elasticsearch的日志存储和查询
- ✅ 缓存服务：Redis缓存，提升系统性能

### 1.3 技术栈
- **框架**：Spring Boot 2.3.12、Spring Cloud Hoxton
- **注册中心**：Nacos（服务注册与配置中心）
- **消息队列**：RabbitMQ（异步处理、削峰填谷）
- **缓存**：Redis（数据缓存、黑名单、限流等）
- **搜索引擎**：Elasticsearch（日志存储与查询）
- **协议**：CMPP 2.0（与运营商通信协议）
- **网络通信**：Netty（高性能NIO框架）

---

## 二、系统架构

### 2.1 模块划分

```
beacon-cloud/
├── beacon-api          # 接口模块：对外提供REST API
├── beacon-strategy     # 策略模块：业务校验和路由选择
├── beacon-smsgateway   # 网关模块：与运营商CMPP协议通信
├── beacon-search       # 搜索模块：ES日志存储
├── beacon-push         # 推送模块：状态报告推送
├── beacon-cache        # 缓存模块：Redis缓存服务
└── beacon-common       # 公共模块：通用工具类
```

### 2.2 架构图

```
客户端请求
    ↓
[beacon-api] 接口模块
    ├─ 参数校验（API Key、IP、签名等）
    ├─ 生成唯一ID（雪花算法）
    └─ 发送到MQ (sms_pre_send_topic)
         ↓
[beacon-strategy] 策略模块
    ├─ 黑名单校验（全局/客户级）
    ├─ 敏感词过滤（DFA算法）
    ├─ 限流校验（每分钟/每小时）
    ├─ 费用校验
    ├─ 路由选择（权重、运营商匹配）
    └─ 发送到MQ (sms_gateway_topic_{channelId})
         ↓
[beacon-smsgateway] 网关模块
    ├─ 监听通道队列
    ├─ Netty发送CMPP协议包
    └─ 接收运营商响应
         ├─ 第一次响应：提交应答（CmppSubmitResp）
         └─ 第二次响应：状态报告（CmppDeliver）
              ↓
         ├─ [beacon-push] 推送模块
         │   └─ HTTP回调客户
         │
         └─ [beacon-search] 搜索模块
             └─ 写入/更新ES日志
```

---

## 三、核心模块详解

### 3.1 beacon-api（接口模块）

**职责**：对外提供短信发送接口，进行基础校验

**核心功能**：
1. **接口入口**：`/sms/single_send` - 单条短信发送
2. **参数校验**：
   - API Key校验：验证客户身份
   - IP白名单校验：防止非法访问
   - 签名校验：数据完整性验证
   - 模板校验：短信模板合法性
   - 费用校验：账户余额检查
3. **ID生成**：使用雪花算法生成唯一序列ID
4. **异步发送**：通过RabbitMQ发送到策略模块

**关键代码位置**：
- `SmsController.java`：控制器
- `CheckFilterContext.java`：校验链上下文
- `filter/impl/`：各种校验过滤器实现

---

### 3.2 beacon-strategy（策略模块）

**职责**：业务策略校验和路由选择

**核心功能**：
1. **策略校验链**：
   - `blackGlobal`：全局黑名单校验（Redis查询）
   - `blackClient`：客户级黑名单校验
   - `dfaDirtyWord`：敏感词过滤（DFA算法）
   - `limitOneMinute`：每分钟限流（Redis ZSet实现）
   - `limitOneHour`：每小时限流
   - `fee`：费用校验
   - `phase`：分阶段发送策略
   - `route`：路由选择策略

2. **路由选择**：
   - 从Redis获取客户绑定的通道列表
   - 根据权重排序（TreeSet）
   - 匹配运营商（全网通/移动/联通/电信）
   - 选择可用通道
   - 动态创建通道专属队列

3. **错误处理**：
   - 校验失败时写入ES日志
   - 推送错误状态报告给客户

**关键代码位置**：
- `StrategyFilterContext.java`：策略上下文（动态加载策略链）
- `filter/impl/RouteStrategyFilter.java`：路由选择
- `filter/impl/BlackGlobalStrategyFilter.java`：黑名单校验
- `filter/impl/DirtyWordDFAStrategyFilter.java`：敏感词过滤

---

### 3.3 beacon-smsgateway（短信网关模块）

**职责**：与运营商网关通信，处理CMPP协议

**核心功能**：
1. **Netty客户端**：
   - 建立TCP长连接
   - 实现CMPP协议编解码器
   - 心跳保活机制

2. **CMPP协议交互**：
   ```
   发送流程：
   1. 建立连接（CMPP_CONNECT）
   2. 提交短信（CMPP_SUBMIT）
   3. 接收提交应答（CMPP_SUBMIT_RESP）- 第一次响应
   4. 接收状态报告（CMPP_DELIVER）- 第二次响应
   ```

3. **消息处理**：
   - 第一次响应：收到`CmppSubmitResp`，存储msgId映射关系
   - 第二次响应：收到`CmppDeliver`状态报告，更新最终状态
   - 使用线程池处理响应（Hippo4j动态线程池）

4. **状态报告处理**：
   - 判断是否需要回调（查询Redis）
   - 发送到推送模块
   - 发送到死信队列，延迟更新ES

**关键技术点**：
- **CMPP协议**：中国移动点对点协议，用于SP与ISMG通信
- **Netty**：高性能NIO框架，处理高并发连接
- **序列号管理**：自增序列，用于请求-响应匹配
- **临时存储**：使用内存Map存储序列号与消息的映射（可优化为Redis）

**关键代码位置**：
- `NettyClient.java`：Netty客户端
- `CMPPHandler.java`：协议处理器
- `SmsGatewayListener.java`：MQ消息监听
- `DeliverRunnable.java`：状态报告处理

---

### 3.4 beacon-push（推送模块）

**职责**：将状态报告推送给客户

**核心功能**：
1. **HTTP回调**：
   - 从`StandardReport`获取客户回调地址
   - 使用RestTemplate发送POST请求
   - 判断响应是否为"SUCCESS"

2. **重试机制**：
   - 失败后发送到延迟队列
   - 重试间隔：0s、15s、30s、60s、300s
   - 最多重试5次

3. **延迟队列**：
   - 使用RabbitMQ延迟插件
   - 实现消息延迟投递

**关键代码位置**：
- `PushReportListener.java`：推送监听器

---

### 3.5 beacon-search（搜索模块）

**职责**：将短信日志存储到Elasticsearch

**核心功能**：
1. **日志写入**：
   - 监听`sms_write_log_topic`队列
   - 按年份创建索引：`sms_submit_log_{year}`
   - 使用序列ID作为文档ID

2. **日志更新**：
   - 监听死信队列（延迟10秒）
   - 更新状态报告信息
   - 更新最终发送状态

**关键代码位置**：
- `SmsWriteLogListener.java`：写入日志
- `SmsUpdateLogListener.java`：更新日志
- `SearchService.java`：ES操作服务

---

### 3.6 beacon-cache（缓存模块）

**职责**：提供Redis缓存服务

**核心功能**：
1. **统一缓存接口**：
   - 提供HTTP接口操作Redis
   - 封装常用操作（String、Hash、Set、ZSet）

2. **缓存内容**：
   - 客户信息（API Key、余额、回调地址等）
   - 通道信息（通道ID、号码、运营商等）
   - 黑名单（全局/客户级）
   - 限流数据（ZSet存储时间戳）

**关键代码位置**：
- `CacheController.java`：缓存控制器

---

## 四、业务流程详解

### 4.1 短信发送完整流程

```
1. 【客户端】调用API
   POST /sms/single_send
   {
     "apikey": "xxx",
     "mobile": "13800138000",
     "text": "验证码：1234",
     "sign": "签名"
   }

2. 【接口模块】基础校验
   ├─ API Key校验 → Redis查询客户信息
   ├─ IP白名单校验
   ├─ 签名校验
   ├─ 模板校验
   └─ 费用校验

3. 【接口模块】生成唯一ID
   └─ 雪花算法生成sequenceId

4. 【接口模块】发送到MQ
   └─ RabbitMQ: sms_pre_send_topic

5. 【策略模块】策略校验链
   ├─ 全局黑名单校验
   ├─ 客户黑名单校验
   ├─ 敏感词过滤（DFA算法）
   ├─ 限流校验
   │   ├─ 每分钟限流：Redis ZSet
   │   └─ 每小时限流
   ├─ 费用校验
   └─ 路由选择
       ├─ 查询客户绑定的通道（Redis Set）
       ├─ 按权重排序
       ├─ 匹配运营商
       └─ 选择可用通道

6. 【策略模块】发送到网关队列
   └─ RabbitMQ: sms_gateway_topic_{channelId}

7. 【网关模块】CMPP协议发送
   ├─ 监听通道队列
   ├─ 封装CMPP Submit包
   ├─ Netty发送到运营商
   └─ 存储序列号映射（内存Map）

8. 【网关模块】接收第一次响应
   └─ CMPP_SUBMIT_RESP（提交应答）
       ├─ 获取msgId
       └─ 存储msgId与序列号映射

9. 【网关模块】接收第二次响应
   └─ CMPP_DELIVER（状态报告）
       ├─ 解析状态（DELIVRD=成功）
       ├─ 更新ES日志（延迟队列）
       └─ 推送状态报告（如果需要）

10. 【推送模块】HTTP回调客户
    └─ POST http://客户回调地址
        ├─ 成功 → 结束
        └─ 失败 → 延迟重试（最多5次）

11. 【搜索模块】日志存储
    ├─ 写入ES：短信发送信息
    └─ 更新ES：状态报告信息
```

### 4.2 关键业务逻辑

#### 4.2.1 路由选择算法
```java
// 1. 获取客户绑定的所有通道（Redis Set）
Set<Map> clientChannels = redis.smember("client:channel:" + clientId);

// 2. 按权重排序（TreeSet，降序）
TreeSet<Map> sortedChannels = new TreeSet<>((o1, o2) -> {
    int w1 = o1.get("weight");
    int w2 = o2.get("weight");
    return w2 - w1; // 降序
});

// 3. 遍历选择通道
for (Map channel : sortedChannels) {
    // 检查通道是否可用
    if (channel.get("isAvailable") == 0) {
        // 检查运营商匹配
        if (channel是全网通 || channel运营商 == 手机号运营商) {
            // 选择该通道
            break;
        }
    }
}
```

#### 4.2.2 限流实现（Redis ZSet）
```java
// 每分钟限流
String key = "limit:minute:" + clientId + ":" + mobile;
long now = System.currentTimeMillis();
// 1. 添加当前时间戳
redis.zadd(key, now, now);
// 2. 移除1分钟前的数据
redis.zremrangeByScore(key, 0, now - 60000);
// 3. 统计1分钟内的数量
long count = redis.zcard(key);
if (count > 限制数量) {
    throw new LimitException();
}
```

#### 4.2.3 敏感词过滤（DFA算法）
```java
// DFA（确定性有限自动机）算法
// 优势：只需要遍历一次文本，时间复杂度O(n)
Set<String> dirtyWords = DFAUtil.getDirtyWord(text);
if (dirtyWords != null && dirtyWords.size() > 0) {
    throw new DirtyWordException();
}
```

---

## 五、技术亮点

### 5.1 微服务架构
- **服务拆分**：按业务领域拆分，职责清晰
- **服务注册**：Nacos服务发现
- **配置中心**：Nacos动态配置，支持热更新
- **服务通信**：OpenFeign远程调用

### 5.2 异步处理
- **消息队列**：RabbitMQ实现异步解耦
- **削峰填谷**：高并发请求通过MQ缓冲
- **可靠性**：手动ACK，保证消息不丢失

### 5.3 高性能
- **Netty**：NIO框架，支持高并发连接
- **线程池**：Hippo4j动态线程池，灵活调整
- **Redis缓存**：减少数据库访问，提升响应速度
- **连接池**：TCP长连接，减少连接开销

### 5.4 可扩展性
- **策略模式**：校验策略可插拔，易于扩展
- **动态路由**：根据权重、运营商动态选择通道
- **多通道支持**：支持多个运营商通道，负载均衡

### 5.5 可靠性
- **重试机制**：状态报告推送失败自动重试
- **死信队列**：处理失败消息
- **延迟队列**：延迟更新ES，保证最终一致性

---

## 六、面试重点问题与答案

### 6.1 项目概述类

**Q1：介绍一下这个项目？**
```
A：这是一个企业级分布式短信平台系统，主要用于处理大并发量的短信发送业务。
   系统采用微服务架构，包含6个核心模块：
   - 接口模块：对外提供REST API，进行基础校验
   - 策略模块：业务校验（黑名单、敏感词、限流）和路由选择
   - 网关模块：通过CMPP协议与运营商通信
   - 搜索模块：ES日志存储
   - 推送模块：状态报告回调
   - 缓存模块：Redis缓存服务
   
   技术栈：Spring Cloud、Nacos、RabbitMQ、Redis、Elasticsearch、Netty、CMPP协议
```

**Q2：项目的核心业务是什么？**
```
A：核心业务是短信发送，包括：
   1. 接收客户请求，进行多维度校验
   2. 选择合适的发送通道
   3. 通过CMPP协议发送到运营商
   4. 接收状态报告并推送给客户
   5. 存储发送日志到ES
```

---

### 6.2 架构设计类

**Q3：为什么采用微服务架构？**
```
A：
1. 业务解耦：每个模块职责单一，互不影响
2. 独立部署：可以单独升级某个模块，不影响其他服务
3. 技术选型灵活：不同模块可以用不同技术栈
4. 扩展性强：可以针对高负载模块单独扩容
5. 团队协作：不同团队可以并行开发不同模块
```

**Q4：模块之间是如何通信的？**
```
A：主要通过两种方式：
   1. 消息队列（RabbitMQ）：用于异步通信，实现解耦
      - 接口模块 → 策略模块：sms_pre_send_topic
      - 策略模块 → 网关模块：sms_gateway_topic_{channelId}
      - 网关模块 → 推送模块：sms_push_report_topic
      - 网关模块 → 搜索模块：sms_write_log_topic
   
   2. OpenFeign：用于同步调用，如策略模块调用缓存模块
```

**Q5：为什么使用RabbitMQ而不是其他消息队列？**
```
A：
1. 可靠性高：支持持久化、确认机制，保证消息不丢失
2. 功能丰富：支持延迟队列、死信队列等高级特性
3. 管理方便：有可视化管理界面
4. 社区成熟：Spring对RabbitMQ支持好
5. 性能满足需求：对于短信这种业务，TPS要求不是极高，但要求可靠性
```

---

### 6.3 技术实现类

**Q6：限流是如何实现的？**
```
A：使用Redis ZSet实现滑动窗口限流：

   1. 每分钟限流：
      - Key：limit:minute:{clientId}:{mobile}
      - Value：时间戳
      - 每次请求时：
        * 添加当前时间戳到ZSet
        * 移除1分钟前的数据
        * 统计当前ZSet中的数量
        * 如果超过限制，抛出异常
   
   2. 优势：
      - 滑动窗口，更精确
      - 基于Redis，性能好
      - 支持分布式限流
   
   3. 也可以使用令牌桶算法：
      - Redis INCR + EXPIRE实现
      - 或者使用Redisson的RateLimiter
```

**Q7：敏感词过滤为什么用DFA算法？**
```
A：
1. 性能优势：
   - 传统方法：每个敏感词都要遍历整个文本，时间复杂度O(n*m)
   - DFA算法：只需要遍历一次文本，时间复杂度O(n)
   
2. 实现原理：
   - 构建敏感词树（Trie树）
   - 从文本开头开始匹配
   - 如果匹配到叶子节点，说明包含敏感词
   
3. 实际应用：
   - 使用Hutool的WordTree或自定义DFA工具类
   - 初始化时构建字典树
   - 查询时一次遍历即可
```

**Q8：路由选择是如何实现的？**
```
A：
1. 数据存储：
   - Redis存储客户-通道绑定关系（Set）
   - 每个通道有：通道ID、权重、运营商、是否可用等属性
   
2. 选择算法：
   - 获取客户绑定的所有通道
   - 按权重降序排序（TreeSet）
   - 遍历通道列表：
     * 检查通道是否可用
     * 检查运营商是否匹配（全网通或特定运营商）
   - 选择第一个满足条件的通道
   
3. 动态队列：
   - 为每个通道创建专属队列：sms_gateway_topic_{channelId}
   - 使用AmqpAdmin动态声明队列
```

**Q9：CMPP协议交互流程是什么？**
```
A：CMPP（中国移动点对点协议）交互流程：

   1. 建立连接：
      - 发送CMPP_CONNECT请求
      - 接收CMPP_CONNECT_RESP响应
      - 连接成功后，保持长连接
   
   2. 发送短信：
      - 封装CMPP_SUBMIT包（包含手机号、内容、源号码等）
      - 通过Netty发送到运营商
      - 生成序列号（sequenceId），用于匹配请求-响应
   
   3. 第一次响应（提交应答）：
      - 接收CMPP_SUBMIT_RESP
      - 获取msgId（运营商生成的短信ID）
      - 存储sequenceId → msgId映射关系
   
   4. 第二次响应（状态报告）：
      - 接收CMPP_DELIVER
      - 判断是否为状态报告（Registered_Delivery=1）
      - 获取最终状态（DELIVRD=成功，其他=失败）
      - 根据msgId找到原始消息，更新状态
   
   5. 心跳保活：
      - 定时发送CMPP_ACTIVE_TEST
      - 接收CMPP_ACTIVE_TEST_RESP
```

**Q10：为什么使用Netty而不是传统的BIO？**
```
A：
1. 性能优势：
   - Netty基于NIO，非阻塞IO，可以处理更多并发连接
   - 传统BIO一个线程一个连接，资源消耗大
   
2. 高并发支持：
   - Netty使用Reactor模式，单线程可以处理多个连接
   - 对于短信网关，需要同时维护多个通道连接
   
3. 易于扩展：
   - Netty提供了Pipeline机制，可以灵活添加编解码器
   - 方便实现CMPP协议的编解码
   
4. 成熟稳定：
   - Netty是业界成熟的高性能网络框架
   - 很多开源项目都在使用
```

---

### 6.4 问题解决类

**Q11：如何保证消息不丢失？**
```
A：多层保障机制：

   1. 消息持久化：
      - RabbitMQ队列和消息都设置为持久化
      - 服务器重启后消息不丢失
   
   2. 生产者确认：
      - 使用confirm模式，确保消息到达MQ
      - 如果失败，可以重试发送
   
   3. 消费者确认：
      - 使用手动ACK，处理完业务逻辑后再确认
      - 如果处理失败，可以拒绝消息，让MQ重新投递
   
   4. 死信队列：
      - 处理失败的消息进入死信队列
      - 可以人工介入或自动重试
   
   5. 日志记录：
      - 所有关键操作都记录日志
      - 可以通过日志追溯消息流向
```

**Q12：如何保证高可用？**
```
A：
1. 服务注册与发现：
   - 使用Nacos，支持多实例注册
   - 客户端可以负载均衡访问多个实例
   
2. 配置中心：
   - Nacos配置中心，配置热更新
   - 不需要重启服务即可更新配置
   
3. 多通道支持：
   - 一个客户可以绑定多个通道
   - 某个通道故障，可以自动切换到其他通道
   
4. 缓存高可用：
   - Redis主从或集群模式
   - 缓存故障时，可以降级查询数据库
   
5. 消息队列高可用：
   - RabbitMQ集群模式
   - 消息镜像，多节点备份
```

**Q13：如何解决分布式事务问题？**
```
A：这个项目采用了最终一致性方案：

   1. 不使用强一致性事务：
      - 短信发送是异步流程，不需要强一致性
      - 可以接受短暂的数据不一致
   
   2. 最终一致性保障：
      - 通过消息队列保证消息最终被处理
      - 失败重试机制，保证最终成功
      - 状态报告延迟队列，保证ES最终更新
   
   3. 补偿机制：
      - 状态报告推送失败，延迟重试
      - ES日志更新失败，可以通过状态报告重新更新
   
   4. 幂等性：
      - 使用唯一ID（雪花算法）保证幂等
      - 相同ID的消息只处理一次
```

**Q14：如何优化系统性能？**
```
A：
1. 缓存优化：
   - Redis缓存客户信息、通道信息
   - 减少数据库查询
   - 黑名单、限流数据都存储在Redis
   
2. 异步处理：
   - 使用MQ异步处理，快速响应客户
   - 短信发送、日志存储都是异步的
   
3. 连接池：
   - Netty长连接，复用连接
   - Redis连接池、HTTP连接池
   
4. 线程池优化：
   - 使用Hippo4j动态线程池
   - 根据负载动态调整线程数
   
5. 数据库优化：
   - ES按年份分片，避免单个索引过大
   - 使用唯一ID作为文档ID，提高查询效率
```

---

### 6.5 业务理解类

**Q15：黑名单是如何管理的？**
```
A：
1. 分类：
   - 全局黑名单：所有客户都不能发送
   - 客户黑名单：特定客户的黑名单
   
2. 存储：
   - Redis存储，Key：black:{mobile}，Value：1
   - 查询时直接get，性能高
   
3. 管理：
   - 后台管理系统可以添加/删除黑名单
   - 通过缓存模块的HTTP接口操作Redis
   
4. 校验：
   - 策略模块中，全局黑名单优先校验
   - 如果命中，直接拒绝，不继续后续校验
```

**Q16：状态报告是如何处理的？**
```
A：
1. 接收状态报告：
   - 网关模块通过Netty接收CMPP_DELIVER
   - 判断是否为状态报告（Registered_Delivery=1）
   - 解析状态码（DELIVRD=成功）
   
2. 判断是否需要回调：
   - 查询Redis，获取客户的isCallback标志
   - 如果需要回调，再查询callbackUrl
   
3. 推送状态报告：
   - 封装StandardReport对象
   - 发送到推送模块的MQ队列
   
4. HTTP回调：
   - 推送模块使用RestTemplate调用客户回调地址
   - 判断响应是否为"SUCCESS"
   - 失败则进入延迟队列重试（最多5次）
   
5. 日志更新：
   - 发送到死信队列，延迟10秒
   - 搜索模块监听死信队列，更新ES中的状态
```

**Q17：通道是如何管理的？**
```
A：
1. 通道信息：
   - 通道ID、通道号码、运营商、权重、是否可用等
   - 存储在Redis Hash结构中
   
2. 客户-通道绑定：
   - 一个客户可以绑定多个通道
   - 存储在Redis Set中：client:channel:{clientId}
   - Set中存储通道对象（包含权重等信息）
   
3. 路由选择：
   - 获取客户绑定的所有通道
   - 按权重排序
   - 选择可用且运营商匹配的通道
   
4. 动态切换：
   - 通道不可用时，自动选择下一个通道
   - 支持通道的启用/禁用
```

---

### 6.6 技术深度类

**Q18：雪花算法是如何实现的？**
```
A：雪花算法生成64位ID：
   - 1位：符号位（0）
   - 41位：时间戳（毫秒级）
   - 10位：机器ID（5位数据中心ID + 5位机器ID）
   - 12位：序列号（同一毫秒内的自增序列）
   
   优势：
   - 趋势递增，有利于数据库索引
   - 性能高，不需要网络请求
   - 可以保证全局唯一
   
   实现：
   - 使用Hutool的IdUtil或自定义实现
   - 注意时钟回拨问题，需要处理
```

**Q19：ES日志是如何设计的？**
```
A：
1. 索引设计：
   - 按年份分片：sms_submit_log_{year}
   - 避免单个索引过大，提高查询性能
   
2. 文档ID：
   - 使用序列ID（雪花算法生成的唯一ID）
   - 保证文档唯一性，可以幂等更新
   
3. 存储内容：
   - 首次写入：短信发送信息（手机号、内容、时间等）
   - 延迟更新：状态报告信息（最终状态、错误信息等）
   
4. 查询场景：
   - 按手机号查询发送记录
   - 按客户ID查询发送记录
   - 按时间范围查询
```

**Q20：如何处理高并发？**
```
A：
1. 架构层面：
   - 微服务拆分，可以单独扩容高负载模块
   - 使用MQ削峰填谷，缓冲高并发请求
   
2. 缓存：
   - Redis缓存热点数据，减少数据库访问
   - 限流数据、黑名单都存储在Redis
   
3. 异步处理：
   - 短信发送是异步的，快速响应客户
   - 日志存储也是异步的，不阻塞主流程
   
4. 连接池：
   - Netty长连接，复用连接
   - 线程池处理响应，避免阻塞
   
5. 负载均衡：
   - Nacos服务发现，支持多实例
   - 客户端负载均衡访问
```

---

## 七、项目总结

### 7.1 项目收获
1. **架构设计能力**：理解了微服务架构的设计思想
2. **技术深度**：掌握了Netty、CMPP协议、消息队列等核心技术
3. **问题解决能力**：学会了如何设计高可用、高性能的系统
4. **业务理解能力**：理解了短信行业的业务流程

### 7.2 可优化点
1. **序列号映射**：当前使用内存Map，可以改为Redis，提高可靠性
2. **监控告警**：可以接入Prometheus + Grafana，实时监控系统状态
3. **链路追踪**：可以使用SkyWalking，追踪消息在系统中的流转
4. **数据库优化**：如果有数据库，可以考虑读写分离、分库分表
5. **限流升级**：可以使用Redis + Lua脚本，更精确的限流

### 7.3 面试建议
1. **熟悉业务流程**：能够清晰描述短信发送的完整流程
2. **理解技术选型**：知道为什么选择某个技术，有什么优势
3. **准备问题答案**：针对上述问题，准备自己的回答
4. **准备扩展问题**：如"如果让你优化这个系统，你会怎么做？"
5. **代码准备**：能够画出架构图，解释关键代码逻辑

---

## 八、项目亮点总结（一句话总结）

1. ✅ **微服务架构**：6个模块，职责清晰，易于扩展
2. ✅ **异步处理**：MQ削峰填谷，提升系统吞吐量
3. ✅ **高性能**：Netty + Redis，支持高并发
4. ✅ **可靠保证**：手动ACK + 重试机制，保证消息不丢失
5. ✅ **智能路由**：权重 + 运营商匹配，动态选择通道
6. ✅ **策略模式**：校验策略可插拔，易于扩展新策略
7. ✅ **协议对接**：实现CMPP协议，与运营商网关通信
8. ✅ **日志存储**：ES分片存储，支持大数据量查询

---

**祝你面试顺利！** 🚀

