# 短信平台系统 - 面试问答快速版

## 一、项目介绍（3分钟版本）

**项目名称**：Beacon短信平台系统

**项目定位**：企业级分布式短信服务平台，处理大并发量短信发送

**技术架构**：
- Spring Cloud微服务架构
- 6个核心模块：接口、策略、网关、搜索、推送、缓存
- 技术栈：Nacos、RabbitMQ、Redis、Elasticsearch、Netty、CMPP协议

**核心流程**：
```
客户端 → 接口模块（校验） → 策略模块（业务校验+路由） 
→ 网关模块（CMPP协议） → 运营商 → 状态报告 → 推送模块 → 客户回调
```

---

## 二、核心模块职责（必须掌握）

| 模块 | 职责 | 关键技术 |
|------|------|----------|
| **beacon-api** | 对外接口，基础校验 | 参数校验、API Key、IP白名单 |
| **beacon-strategy** | 业务校验、路由选择 | 黑名单、敏感词、限流、路由算法 |
| **beacon-smsgateway** | 与运营商通信 | Netty、CMPP协议、状态报告处理 |
| **beacon-search** | 日志存储 | Elasticsearch、按年份分片 |
| **beacon-push** | 状态报告推送 | HTTP回调、延迟重试 |
| **beacon-cache** | Redis缓存服务 | 统一缓存接口 |

---

## 三、面试高频问题（必须会答）

### Q1：介绍一下这个项目？
**标准答案**：
```
这是一个企业级分布式短信平台，采用微服务架构，主要处理大并发量短信发送。

系统包含6个核心模块：
1. 接口模块：对外提供REST API，进行API Key、IP、签名等基础校验
2. 策略模块：业务校验（黑名单、敏感词、限流）和路由选择
3. 网关模块：通过CMPP协议与运营商网关通信
4. 搜索模块：将发送日志存储到Elasticsearch
5. 推送模块：将状态报告通过HTTP回调推送给客户
6. 缓存模块：提供Redis缓存服务

技术栈：Spring Cloud、Nacos、RabbitMQ、Redis、Elasticsearch、Netty

核心流程：客户端请求 → 接口校验 → 策略校验 → 选择通道 → CMPP发送 → 接收状态报告 → 回调客户
```

---

### Q2：短信发送的完整流程是什么？
**标准答案**：
```
1. 客户端调用接口 /sms/single_send，传入手机号、内容、API Key等
2. 接口模块校验：API Key、IP白名单、签名、模板、费用
3. 生成唯一ID（雪花算法），发送到MQ：sms_pre_send_topic
4. 策略模块监听MQ，执行校验链：
   - 全局黑名单校验（Redis查询）
   - 客户黑名单校验
   - 敏感词过滤（DFA算法）
   - 限流校验（Redis ZSet实现滑动窗口）
   - 路由选择（按权重、运营商匹配选择通道）
5. 发送到网关模块的通道队列：sms_gateway_topic_{channelId}
6. 网关模块封装CMPP协议包，通过Netty发送到运营商
7. 接收两次响应：
   - 第一次：提交应答（获取msgId）
   - 第二次：状态报告（获取最终状态）
8. 如果客户需要回调，推送模块通过HTTP回调客户
9. 搜索模块将日志写入ES，并更新状态报告
```

---

### Q3：限流是如何实现的？
**标准答案**：
```
使用Redis ZSet实现滑动窗口限流：

每分钟限流：
1. Key：limit:minute:{clientId}:{mobile}
2. 每次请求时：
   - 添加当前时间戳到ZSet（score和value都是时间戳）
   - 移除1分钟前的数据（zremrangeByScore）
   - 统计当前ZSet中的数量（zcard）
   - 如果超过限制（比如5条），抛出限流异常

优势：
- 滑动窗口，比固定窗口更精确
- 基于Redis，性能好，支持分布式
- 自动过期，不需要手动清理

也可以使用令牌桶算法：
- Redis INCR + EXPIRE
- 或使用Redisson的RateLimiter
```

---

### Q4：敏感词过滤为什么用DFA算法？
**标准答案**：
```
DFA（确定性有限自动机）算法的优势：

1. 性能优势：
   - 传统方法：每个敏感词都要遍历整个文本，时间复杂度O(n*m)
   - DFA算法：只需要遍历一次文本，时间复杂度O(n)

2. 实现原理：
   - 构建敏感词字典树（Trie树）
   - 从文本开头开始匹配
   - 如果匹配到叶子节点，说明包含敏感词

3. 实际应用：
   - 使用Hutool的WordTree或自定义DFA工具类
   - 初始化时构建字典树（一次性）
   - 查询时一次遍历即可（高效）
```

---

### Q5：路由选择是如何实现的？
**标准答案**：
```
路由选择算法：

1. 数据存储：
   - Redis存储客户-通道绑定关系（Set结构）
   - 每个通道包含：通道ID、权重、运营商类型、是否可用等

2. 选择流程：
   - 从Redis获取客户绑定的所有通道：client:channel:{clientId}
   - 按权重降序排序（使用TreeSet，自定义Comparator）
   - 遍历通道列表：
     * 检查通道是否可用（isAvailable == 0）
     * 检查运营商匹配（全网通或特定运营商匹配）
   - 选择第一个满足条件的通道

3. 动态队列：
   - 为每个通道创建专属队列：sms_gateway_topic_{channelId}
   - 使用AmqpAdmin动态声明队列
   - 发送到对应通道的队列

4. 容错处理：
   - 如果没有可用通道，抛出异常
   - 记录错误日志，推送失败状态报告
```

---

### Q6：CMPP协议交互流程是什么？
**标准答案**：
```
CMPP（中国移动点对点协议）交互流程：

1. 建立连接：
   - 发送CMPP_CONNECT请求（包含账号、密码）
   - 接收CMPP_CONNECT_RESP响应
   - 连接成功后，保持TCP长连接

2. 发送短信：
   - 封装CMPP_SUBMIT包（包含手机号、内容、源号码等）
   - 生成序列号（sequenceId），用于匹配请求-响应
   - 通过Netty发送到运营商网关

3. 第一次响应（提交应答）：
   - 接收CMPP_SUBMIT_RESP
   - 获取msgId（运营商生成的短信ID）
   - 存储sequenceId → msgId映射关系（内存Map，可优化为Redis）

4. 第二次响应（状态报告）：
   - 接收CMPP_DELIVER
   - 判断是否为状态报告（Registered_Delivery == 1）
   - 解析状态码：
     * DELIVRD = 成功
     * 其他 = 失败
   - 根据msgId找到原始消息，更新状态

5. 心跳保活：
   - 定时发送CMPP_ACTIVE_TEST
   - 接收CMPP_ACTIVE_TEST_RESP
   - 保证连接不中断
```

---

### Q7：为什么使用Netty？
**标准答案**：
```
1. 性能优势：
   - Netty基于NIO（非阻塞IO），可以处理更多并发连接
   - 传统BIO一个线程一个连接，资源消耗大
   - Netty使用Reactor模式，单线程可以处理多个连接

2. 高并发支持：
   - 对于短信网关，需要同时维护多个通道连接
   - Netty可以轻松支持数千个并发连接

3. 易于扩展：
   - Netty提供了Pipeline机制，可以灵活添加编解码器
   - 方便实现CMPP协议的编解码（CMPPEncoder、CMPPDecoder）

4. 成熟稳定：
   - Netty是业界成熟的高性能网络框架
   - 很多开源项目都在使用（Dubbo、RocketMQ等）
```

---

### Q8：如何保证消息不丢失？
**标准答案**：
```
多层保障机制：

1. 消息持久化：
   - RabbitMQ队列设置为持久化（durable=true）
   - 消息设置为持久化（deliveryMode=2）
   - 服务器重启后消息不丢失

2. 生产者确认：
   - 使用confirm模式，确保消息到达MQ
   - 如果失败，可以重试发送
   - 使用CorrelationData关联消息ID

3. 消费者确认：
   - 使用手动ACK（manual acknowledgment）
   - 处理完业务逻辑后再确认
   - 如果处理失败，可以拒绝消息（nack），让MQ重新投递

4. 死信队列：
   - 处理失败的消息进入死信队列
   - 可以人工介入或自动重试
   - 网关模块使用死信队列延迟更新ES

5. 日志记录：
   - 所有关键操作都记录日志
   - 可以通过日志追溯消息流向
```

---

### Q9：状态报告是如何处理的？
**标准答案**：
```
状态报告处理流程：

1. 接收状态报告：
   - 网关模块通过Netty接收CMPP_DELIVER消息
   - 判断是否为状态报告（Registered_Delivery == 1）
   - 解析状态码：DELIVRD=成功，其他=失败

2. 判断是否需要回调：
   - 查询Redis，获取客户的isCallback标志
   - 如果需要回调，再查询callbackUrl

3. 推送状态报告：
   - 封装StandardReport对象
   - 发送到推送模块的MQ队列：sms_push_report_topic

4. HTTP回调：
   - 推送模块使用RestTemplate调用客户回调地址
   - 发送POST请求，Body是JSON格式的状态报告
   - 判断响应是否为"SUCCESS"

5. 重试机制：
   - 如果回调失败，进入延迟队列重试
   - 重试间隔：0s、15s、30s、60s、300s
   - 最多重试5次

6. 日志更新：
   - 发送到死信队列，延迟10秒
   - 搜索模块监听死信队列，更新ES中的状态报告
```

---

### Q10：如何保证高可用？
**标准答案**：
```
1. 服务注册与发现：
   - 使用Nacos作为注册中心
   - 支持多实例注册，客户端负载均衡访问

2. 配置中心：
   - Nacos配置中心，配置热更新
   - 不需要重启服务即可更新配置
   - @RefreshScope支持配置动态刷新

3. 多通道支持：
   - 一个客户可以绑定多个通道
   - 某个通道故障，可以自动切换到其他通道
   - 路由选择时会跳过不可用通道

4. 缓存高可用：
   - Redis主从或集群模式
   - 缓存故障时，可以降级查询数据库（如果有）

5. 消息队列高可用：
   - RabbitMQ集群模式
   - 消息镜像，多节点备份
   - 队列和交换机都设置为持久化

6. 异步处理：
   - 使用MQ异步处理，快速响应客户
   - 即使某个模块暂时不可用，消息也会在MQ中等待
```

---

### Q11：如何优化系统性能？
**标准答案**：
```
1. 缓存优化：
   - Redis缓存客户信息、通道信息、黑名单
   - 减少数据库查询
   - 限流数据存储在Redis ZSet

2. 异步处理：
   - 使用MQ异步处理，快速响应客户
   - 短信发送、日志存储都是异步的
   - 接口模块发送到MQ后立即返回，不等待处理结果

3. 连接池：
   - Netty长连接，复用连接，减少连接开销
   - Redis连接池
   - HTTP连接池（RestTemplate）

4. 线程池优化：
   - 使用Hippo4j动态线程池
   - 根据负载动态调整线程数
   - 网关模块使用线程池处理运营商响应

5. 数据库优化：
   - ES按年份分片：sms_submit_log_{year}
   - 避免单个索引过大，提高查询效率
   - 使用唯一ID作为文档ID，支持幂等更新

6. 负载均衡：
   - Nacos服务发现，支持多实例
   - 客户端负载均衡访问
```

---

### Q12：如何解决分布式事务问题？
**标准答案**：
```
这个项目采用了最终一致性方案，不使用强一致性事务：

1. 为什么不需要强一致性：
   - 短信发送是异步流程，不需要强一致性
   - 可以接受短暂的数据不一致
   - 最终会通过状态报告保证数据一致

2. 最终一致性保障：
   - 通过消息队列保证消息最终被处理
   - 失败重试机制，保证最终成功
   - 状态报告延迟队列，保证ES最终更新

3. 补偿机制：
   - 状态报告推送失败，延迟重试（最多5次）
   - ES日志更新失败，可以通过状态报告重新更新

4. 幂等性：
   - 使用唯一ID（雪花算法）保证幂等
   - 相同ID的消息只处理一次
   - ES使用序列ID作为文档ID，支持幂等更新

5. 如果必须强一致性：
   - 可以使用Seata分布式事务
   - 或使用本地消息表方案
   - 但会增加系统复杂度，影响性能
```

---

## 四、项目亮点（一句话总结）

1. ✅ **微服务架构**：6个模块，职责清晰，易于扩展
2. ✅ **异步处理**：MQ削峰填谷，提升系统吞吐量
3. ✅ **高性能**：Netty + Redis，支持高并发
4. ✅ **可靠保证**：手动ACK + 重试机制，保证消息不丢失
5. ✅ **智能路由**：权重 + 运营商匹配，动态选择通道
6. ✅ **策略模式**：校验策略可插拔，易于扩展新策略
7. ✅ **协议对接**：实现CMPP协议，与运营商网关通信
8. ✅ **日志存储**：ES分片存储，支持大数据量查询

---

## 五、技术点速查表

| 技术点 | 作用 | 关键实现 |
|--------|------|----------|
| **雪花算法** | 生成唯一ID | 41位时间戳+10位机器ID+12位序列号 |
| **DFA算法** | 敏感词过滤 | Trie树，一次遍历O(n) |
| **Redis ZSet** | 限流实现 | 时间戳作为score，滑动窗口 |
| **CMPP协议** | 运营商通信 | Netty编解码，长连接 |
| **延迟队列** | 重试机制 | RabbitMQ延迟插件 |
| **死信队列** | 失败处理 | 延迟更新ES日志 |
| **策略模式** | 校验链 | Map<String, Filter>动态加载 |
| **权重路由** | 通道选择 | TreeSet排序，运营商匹配 |

---

## 六、常见扩展问题

### Q：如果让你优化这个系统，你会怎么做？

**标准答案**：
```
1. 序列号映射优化：
   - 当前使用内存Map存储sequenceId → msgId映射
   - 可以改为Redis，提高可靠性，支持分布式

2. 监控告警：
   - 接入Prometheus + Grafana
   - 实时监控系统状态（QPS、延迟、错误率）
   - 关键指标告警

3. 链路追踪：
   - 使用SkyWalking或Zipkin
   - 追踪消息在系统中的流转
   - 快速定位问题

4. 数据库优化：
   - 如果有数据库，考虑读写分离、分库分表
   - 数据库连接池优化

5. 限流升级：
   - 使用Redis + Lua脚本，更精确的限流
   - 支持限流规则的动态配置

6. 缓存优化：
   - 增加本地缓存（Caffeine）
   - 减少Redis访问，提高性能

7. 异步化优化：
   - 更多操作异步化
   - 使用CompletableFuture处理并行任务
```

---

## 七、面试准备清单

- [ ] 能够清晰描述项目整体架构
- [ ] 能够画出系统架构图和数据流程图
- [ ] 熟悉每个模块的职责和关键技术
- [ ] 能够详细说明短信发送的完整流程
- [ ] 掌握限流、敏感词、路由等核心算法
- [ ] 理解CMPP协议交互流程
- [ ] 知道如何保证消息不丢失、高可用
- [ ] 准备一些优化建议
- [ ] 熟悉代码中的关键实现细节

---

**祝你面试成功！** 🎉

